{
  "mcpServers": {
    "word-docx-tools": {
      "command": "directofficeword_mcp",
      "env": {
        "MCP_TRANSPORT": "stdio"
      }
    }
  },
  "tools": [
    {
      "name": "open_document",
      "title": "Open Document",
      "description": "Opens a Word document and prepares it for editing. This must be the first tool called.",
      "inputSchema": {
        "type": "object",
        "required": ["file_path"],
        "properties": {
          "file_path": {
            "type": "string",
            "description": "The absolute path to the .docx file."
          }
        }
      }
    },
    {
      "name": "shutdown_word",
      "title": "Shutdown Word",
      "description": "Closes the document and shuts down the Word application instance. Should be called at the end of a session.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "insert_paragraph",
      "title": "Insert Paragraph",
      "description": "Inserts a new paragraph with the given text relative to the object found by the locator.",
      "inputSchema": {
        "type": "object",
        "required": ["locator", "text"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the anchor object."
          },
          "text": {
            "type": "string",
            "description": "The text of the paragraph to insert."
          },
          "position": {
            "type": "string",
            "description": "\"before\" or \"after\" the located object.",
            "default": "after"
          }
        }
      }
    },
    {
      "name": "insert_text",
      "title": "Insert Text",
      "description": "Inserts text with the given text relative to the object found by the locator.",
      "inputSchema": {
        "type": "object",
        "required": ["locator", "text"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the anchor object."
          },
          "text": {
            "type": "string",
            "description": "The text to insert."
          },
          "position": {
            "type": "string",
            "description": "\"before\", \"after\", or \"replace\" the located object.",
            "default": "after"
          },
          "style": {
            "type": "string",
            "description": "Optional, the paragraph style name to apply."
          }
        }
      }
    },
    {
      "name": "get_text_from_cell",
      "title": "Get Text From Cell",
      "description": "Retrieves the text from a single table cell found by the locator.",
      "inputSchema": {
        "type": "object",
        "required": ["locator"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the target cell."
          }
        }
      }
    },
    {
      "name": "delete_object",
      "title": "Delete Object",
      "description": "Deletes the object(s) found by the locator.",
      "inputSchema": {
        "type": "object",
        "required": ["locator"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the target object(s) to delete."
          }
        }
      }
    },
    {
      "name": "get_text",
      "title": "Get Text",
      "description": "Retrieves the text from all objects found by the locator or from a specific range.",
      "inputSchema": {
        "type": "object",
        "required": [],
        "properties": {
          "locator": {
            "type": "object",
            "description": "Optional. The Locator object to find the target object(s)."
          },
          "start_pos": {
            "type": "integer",
            "description": "Optional. The start position of the text range."
          },
          "end_pos": {
            "type": "integer",
            "description": "Optional. The end position of the text range."
          }
        }
      }
    },
    {
      "name": "replace_text",
      "title": "Replace Text",
      "description": "Replaces the text content of the object(s) found by the locator with new text.",
      "inputSchema": {
        "type": "object",
        "required": ["locator", "new_text"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the target object(s) to replace."
          },
          "new_text": {
            "type": "string",
            "description": "The new text to replace the existing content."
          }
        }
      }
    },
    {
      "name": "set_cell_value",
      "title": "Set Cell Value",
      "description": "Sets the text value of a single table cell found by the locator.",
      "inputSchema": {
        "type": "object",
        "required": ["locator", "text"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the target cell."
          },
          "text": {
            "type": "string",
            "description": "The text to set in the cell."
          }
        }
      }
    },
    {
      "name": "create_table",
      "title": "Create Table",
      "description": "Creates a new table at the location specified by the locator.",
      "inputSchema": {
        "type": "object",
        "required": ["locator", "rows", "cols"],
        "properties": {
          "locator": {
            "type": "object",
            "description": "The Locator object to find the anchor point for the new table."
          },
          "rows": {
            "type": "integer",
            "description": "Number of rows for the new table."
          },
          "cols": {
            "type": "integer",
            "description": "Number of columns for the new table."
          }
        }
      }
    }
  ]
}